<%= form_tag("/users/search", method: "get") do %>
  <%= select_tag "organization_id", options_for_select(['All Orgs'] + @organizations_for_select )%>
  <%= submit_tag "Search", { :id => "search_submit_button"}%>
<%end%>
